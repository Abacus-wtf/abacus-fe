import { H2, Font, H3 } from "abacus-ui"
import styled from "styled-components"

export const SectionTitle = styled(H2)`
  ${Font("yotta", "Bluu next")};
  font-weight: bold;
  text-align: center;
`

export const SectionSubTitle = styled(H3)`
  ${Font("peta")}
  font-weight: bold;
  text-align: center;
`
