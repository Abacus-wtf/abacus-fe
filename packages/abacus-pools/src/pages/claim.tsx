import GlobalLayout from "@layouts/index"
import { Claim } from "@sections/index"
import React from "react"

const ClaimPage = (props: any) => (
  <GlobalLayout {...props}>
    <Claim />
  </GlobalLayout>
)

export default ClaimPage
