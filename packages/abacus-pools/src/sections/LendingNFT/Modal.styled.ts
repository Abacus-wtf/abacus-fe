import { Button, Modal } from "abacus-ui"
import styled from "styled-components"

export const StyledModal = styled(Modal)`
  position: relative;
  gap: 16px;
`

export const Container = styled.div`
  display: flex;
  width: 100%;
  flex-direction: column;
  gap: 16px;
`

export const MaxButton = styled(Button)`
  padding: 9px 20px;
`
