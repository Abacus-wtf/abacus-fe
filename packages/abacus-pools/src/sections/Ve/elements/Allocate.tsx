import React from "react"
import { SectionTitle, StyledSection } from "./Ve.styles"

const Allocate = () => (
  <StyledSection order={4}>
    <SectionTitle>Allocate</SectionTitle>
  </StyledSection>
)

export { Allocate }
