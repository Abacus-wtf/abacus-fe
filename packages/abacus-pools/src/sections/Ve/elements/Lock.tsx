import React from "react"
import { SectionTitle, StyledSection } from "./Ve.styles"

const Lock = () => (
  <StyledSection order={3}>
    <SectionTitle>Lock</SectionTitle>
  </StyledSection>
)

export { Lock }
