import { FunctionComponent } from "react";
declare type StatInfoProps = {
    stat: string;
    title: string;
    showEthIcon?: boolean;
};
declare const StatInfo: FunctionComponent<StatInfoProps>;
export default StatInfo;
